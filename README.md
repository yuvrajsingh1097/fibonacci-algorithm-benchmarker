Recursion and the call stack — why naive recursion is elegant but expensive
Memoization — trading space for speed using Python's functools.lru_cache
Dynamic programming — bottom-up iteration, the most efficient approach
Time complexity analysis — O(2ⁿ) vs O(n), visualised with real data
Benchmarking — using time.perf_counter and averaging over multiple runs
Unit testing — parametrized pytest tests against known Fibonacci values




Tech stack

Python 3.10+
matplotlib — plotting
numpy — timing statistics
pytest — unit testing
functools.lru_cache — built-in memoization (no extra library needed)
